//move bat towards player, player has already been detected

//this code is only running once at start for some reason

show_debug_message("bat all day every day");
move_towards_point(target.x, target.y, spd);

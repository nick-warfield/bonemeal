//run some code on unlock, in this case play a gate opening animation and change the collision mask
if (locked == false)
{
    sprite_index = Gate_Open_v02_spr;
    mask_index = Gate_Open_v02_spr;

    //var txt = instance_nearest(x, y, Gate_Txt_obj);
    //txt.message[0] = "It Opened!";
    
    //instance_destroy();
}


/*
if (other.id != spawner && other.id == Character_obj)
{
    scrub = other;
    
    var temp = id;
    with(scrub)
    {
        script_execute(state[s_DAMAGED], 3, 100, temp);
    }
}


/* */
/*  */

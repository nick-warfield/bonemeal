draw_self();

/*
if (facing == RIGHT)
{
    draw_ellipse(x + 64, y + 64, x, y - 64, false);
}
else if (facing == LEFT)
{
    draw_ellipse(x, y - 64, x - 64, y - 64, false);
}
else if (facing == UP)
{
    draw_ellipse(x + 64, y - 64, x - 64, y, false);
}
else if (facing == DOWN)
{
    draw_ellipse(x + 64, y - 64, x - 64, y, false);
}

/* */
/*  */
